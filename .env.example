# Plantilla de variables de entorno para shipiandoenvios_client
# USO: Copiar este archivo a `.env.local` y rellenar los valores en tu entorno local/hosting.
# NO pongas valores secretos reales en este fichero si vas a commitearlo. Prefiere `.env.local` no versionado.

# URL base del API (cliente-side). Debe apuntar al backend (sin /api final)
NEXT_PUBLIC_API_URL=https://api.example.com

# Entorno (development | staging | production)
NEXT_PUBLIC_ENVIRONMENT=development

# Feature flags / configuraciones públicas
NEXT_PUBLIC_ENABLE_ANALYTICS=true
NEXT_PUBLIC_ANALYTICS_ID=UA-XXXXXX-X

# API keys públicas (si las hay). NO ponga secrets aquí sin convicción.
NEXT_PUBLIC_MAPS_API_KEY=pk_XXXXXXXXXXXXXXXXXXXX
NEXT_PUBLIC_SOME_PUBLIC_KEY=public-xxxx

# Sentry/observability: en front es común usar NEXT_PUBLIC_SENTRY_DSN, pero la DSN pública se considera "semi-publica".
NEXT_PUBLIC_SENTRY_DSN=https://public@sentry.io/123456

# Nombre de la cookie de autenticación usada por middleware SSR (coincide con server)
# Si migramos a cookie HttpOnly, el servidor establecerá esta cookie. Aquí solo es un nombre de referencia.
NEXT_PUBLIC_AUTH_COOKIE_NAME=sopy-auth-token

# Nombre de la key usada en localStorage para el token (cliente)
NEXT_PUBLIC_AUTH_TOKEN_KEY=auth-token

# Nombre de la key/cookie usada para guardar el objeto usuario (cliente/server)
NEXT_PUBLIC_USER_KEY=sopy-auth-user

# Nombres de cookie disponibles para el server (no deben contener secretos)
# En el cliente se usan las variables NEXT_PUBLIC_*; el servidor usa AUTH_COOKIE_NAME y USER_COOKIE_NAME
AUTH_COOKIE_NAME=sopy-auth-token
USER_COOKIE_NAME=sopy-user

# Nombre de la cookie CSRF (double-submit) que el servidor establecerá en login.
# Cliente deberá leer esta cookie (no-HttpOnly) y enviar su valor en el header x-csrf-token
NEXT_PUBLIC_AUTH_CSRF_COOKIE_NAME=sopy-csrf

# Opcional: URL de autenticación externa (OAuth/OIDC public client id)
NEXT_PUBLIC_OIDC_CLIENT_ID=your-client-id
NEXT_PUBLIC_OIDC_ISSUER=https://auth.example.com

# NOTAS:
# - No incluyas valores secretos (p. ej. secret keys, JWT_SECRET, refresh token secrets) en archivos versionados.
# - Si necesitas almacenar secrets para el servidor (JWT_SECRET, OAUTH_CLIENT_SECRET, DB_URL), colócalos en
#   `shipiando_api/.env` (no versionado) o en el secret manager de tu plataforma (Vercel, Netlify, Azure, etc.).
# - Añade `.env.local` a .gitignore si no existe (Next.js por defecto lo ignora). Guarda valores reales en tu entorno de despliegue.
